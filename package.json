{
  "name": "euro-master-league",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build step: no se necesita build para archivos estáticos'"
  },
  "devDependencies": {
    "@vercel/static-build": "^1.0.0"
  }
}
