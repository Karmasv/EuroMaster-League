[build]
nixpacksBranch = "main"

[phases.build]
buildCommand = "cd discord-bot && npm ci --prefer-offline --no-audit"

[runtime]
healthcheck = { port = 8080, path = "/health", timeout = 30 }

[variables]
NODE_ENV = "production"

