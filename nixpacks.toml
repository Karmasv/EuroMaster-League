[build]
nixpacksBranch = "main"

[phases.build]
buildCommand = "npm ci --prefer-offline --no-audit"

[runtime]
  [runtime.healthcheck]
  port = 8080
  path = "/health"
  timeout = 30

[variables]
NODE_ENV = "production"

